%nav.space-y-1.px-3
  - navigation_items.each do |item|
    - active = current_page?(item[:path])
    = link_to item[:path], class: "flex items-center gap-3 px-3 py-2 rounded-lg transition-colors #{active ? 'bg-amber-100 text-amber-900 font-medium' : 'text-gray-700 hover:bg-gray-100'}" do
      = lucide_icon(item[:icon], class: "w-5 h-5 flex-shrink-0")
      %span= item[:name]
