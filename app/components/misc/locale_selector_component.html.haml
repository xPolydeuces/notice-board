.locale-selector.flex.items-center.space-x-2
  - available_locales.each do |locale|
    - if locale == current_locale
      %span.text-sm.font-medium.text-gray-900.px-2.py-1.bg-gray-100.rounded
        = locale_name(locale)
    - else
      = link_to locale_name(locale), locale_url(locale), class: "text-sm font-medium text-gray-600 hover:text-gray-900 px-2 py-1 rounded hover:bg-gray-50 transition-colors duration-200"
